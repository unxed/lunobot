# 285 — кастомная отрисовка элементов интерфейса

Первый срез: общий табличный движок геометрии для графических backend’ов с набором `classic`, без изменения поведения по умолчанию. PR [unxed/vtui#128](https://github.com/unxed/vtui/pull/128) слит, merge-коммит `a12e61dd`; post-merge CI #36060770977 успешен. Второй срез [unxed/vtui#129](https://github.com/unxed/vtui/pull/129) добавляет проверку пиксельной эквивалентности общего адаптера и прежней растровой геометрии; CI выполняется. Зависимость f4 пока не обновлялась.

План частей:

1. Общая модель фигур и компиляция в пиксельно выровненные прямоугольники; перенести текущий `classic` без визуальной смены — сделано в PR #128.
2. Подтвердить пиксельную эквивалентность общего `classic`-адаптера для всех image-backed backend’ов — PR #129.
3. Следующая пользовательская часть — переключаемый набор оформления, начать с `rounded` по согласованному дизайну.
