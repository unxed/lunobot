# 285 — кастомная отрисовка элементов интерфейса

Первый срез: общий табличный движок геометрии для графических backend’ов с набором `classic`, без изменения поведения по умолчанию. PR [unxed/vtui#128](https://github.com/unxed/vtui/pull/128) открыт; зависимость f4 пока не обновлялась.

План частей:

1. Общая модель фигур и компиляция в пиксельно выровненные прямоугольники; перенести текущий `classic` без визуальной смены — сделано в PR #128.
2. Подключить единый набор к оставшимся графическим backend’ам и добавить проверки эквивалентности.
3. Следующая пользовательская часть — переключаемый набор оформления (начать с `rounded` или токенов контролов после подтверждения владельца).
