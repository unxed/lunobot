# Issue 904 — статус

11-09-2026: атомарный срез по геометрии развёрнутого окна уже реализован в PR [#1125](https://github.com/unxed/f4/pull/1125), слитом 10-09-2026. На текущем `main` `ToggleHelpZoom` и восстановление размеров используют границу `height - 3`, оставляя строку с верхней рамкой и кнопкой сворачивания видимой; покрытие есть в `internal/dialog/help_search_test.go` и `internal/app/command_palette_help_test.go`.

Дублирующий PR не создавался. Тикет оставлен открытым в состоянии `проверяют` для проверки автором.

Локальные сборки и тесты не запускались согласно LUNOBOT.md.
