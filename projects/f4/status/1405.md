# f4 #1405

1. vtui: `FuzzyMatcher.Strict()` — отключает толерантность к опечаткам (edit distance 0). В работе.
2. f4: настройка `PanelStrictAutoFilter` (катaлог f4:config), подключить в `fastFindMatch` (internal/panel/list.go), поднять зависимость vtui.
