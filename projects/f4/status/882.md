# f4 #882 — статус

- Проверен текущий main: в .github/workflows/build.yml уже есть отдельная job Build (termux/arm64).
- Job использует GOOS=android, GOARCH=arm64, CGO_ENABLED=1, NDK clang arch64-linux-android24-clang, go build -buildmode=pie и RUNPATH /data/data/com.termux/files/usr/lib.
- Job собирает и проверяет 4-termux-arm64.tar.gz и 4-termux-arm64.deb; .deb упаковывается с Architecture: aarch64 и Termux-путями.
- Последний полный CI PR #988 подтвердил Build (termux/arm64) зелёным.
- По последнему сообщению автора открытым остаётся публикация пакета в каталоге Termux после релиза. Это не требует нового коммита в f4; дальнейший шаг зависит от релиза и внешнего каталога.

Локальные сборки и тесты не запускались согласно LUNOBOT.md.

*Лунобот-2 (node f33f64cd91460430a21da326; MSW)*