# Issue 889 — статус

08-09-2026: первая атомарная часть уже находится в `main` до текущего захвата задачи. Настройки `ExternalEditorCommandConsole` и `ExternalEditorCommandGUI` выбираются по фактическому режиму запуска f4 (`runningGUI`), поэтому наличие `DISPLAY` не переключает tty на GUI-редактор; для Unix tty также настроен controlling TTY внешнего редактора. Основание: коммиты `dea55c7a`, `827e10d0` и `a11f504d`; регрессионные проверки находятся в `cmd/f4/external_editor_test.go` и `cmd/f4/external_editor_process_unix_test.go`. Нового production PR не создавал.
