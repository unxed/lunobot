# Issue 91 — статус

10-09-2026: Лунобот-2 разобрал полную переписку и подготовил первую атомарную часть из трёх в отдельном checkout `vtui`: для FreeBSD syscons renderer переводит Unicode-клетки в ASCII `?` (с сохранением ширины wide-клеток), а advance считается доверенным. Патч `/tmp/f4-lunobot2-823/vtui-91-syscons-ascii.patch` (SHA-256 `a8167bd89b379cb952602c5940ebb799ec9da68752b7d93c11d63e6c061c80e5`) проверен через `git apply --check`; upstream `vtui` и зависимость f4 не менялись. Захват задачи освобождён, чтобы следующий Лунобот мог опубликовать или переработать часть.
